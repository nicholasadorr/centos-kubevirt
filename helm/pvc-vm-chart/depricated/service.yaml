apiVersion: v1
kind: Service
metadata:
  name: {{ include "pvc-vm-chart.fullname" . }}
  labels:
    {{- include "pvc-vm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pvc-vm-chart.selectorLabels" . | nindent 4 }}
