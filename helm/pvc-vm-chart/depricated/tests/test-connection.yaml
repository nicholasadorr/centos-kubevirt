apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pvc-vm-chart.fullname" . }}-test-connection"
  labels:
{{ include "pvc-vm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pvc-vm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
