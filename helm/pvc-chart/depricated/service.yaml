apiVersion: v1
kind: Service
metadata:
  name: {{ include "pvc-chart.fullname" . }}
  labels:
    {{- include "pvc-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pvc-chart.selectorLabels" . | nindent 4 }}
